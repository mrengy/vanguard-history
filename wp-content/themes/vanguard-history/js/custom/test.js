jQuery(document).ready(function($){

/*
// change "world" to "Santa Clara" in anchor tags
$('a').each(function(){
    var text = $(this).text();
    $(this).text(text.replace('world', 'Santa Clara'));
  });
*/
});
